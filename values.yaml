# Default values for replicated-starter-kots.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

github_pat: '{{repl ConfigOption "github_pat"}}'
resources:
  limits:
    memory: '256Mi'
    cpu: '500m'
